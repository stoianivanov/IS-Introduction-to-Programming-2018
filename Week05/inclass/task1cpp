#include <iostream>

using namespace std;


int main()
{
    int number;
    int count= 0;
    int sum =0;

    while(count < 5){
        cin >> number;
        if( number > 10 && number < 5555){
            ++count;
            cout << number;
            sum += number;// sum = sum + number;
        }

    }
    cout << "Sum " << sum;
    return 0;
}
